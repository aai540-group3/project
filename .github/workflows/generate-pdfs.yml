name: Generate PDFs

on:
  push:
    paths:
      - ".github/workflows/generate-pdfs.yml"
      - "docs/design-document/Final_Project_Team_3_Deliverable_1.tex"
      - "docs/design-document/references.bib"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

env:
  PACKAGES: packages-tex.txt

jobs:
  generate-pdfs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332 # v4.1.7
        with:
          fetch-depth: 1

      - name: Cache apt packages
        uses: actions/cache@0c45773b623bea8c8e75f6c82b208c3cf94ea4f9 # v4.0.2
        with:
          path: /var/cache/apt/archives
          key: ${{ runner.os }}-apt-${{ hashFiles(env.PACKAGES) }}-
          restore-keys: |
            ${{ runner.os }}-apt-

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get --yes install --no-install-recommends $(cat ${{ env.PACKAGES }})

      - name: Generate PDFs
        run: |
          pdflatex -debug docs/design-document/Final_Project_Team_3_Deliverable_1.tex
          biber --debug docs/design-document/Final_Project_Team_3_Deliverable_1
          pdflatex docs/design-document/Final_Project_Team_3_Deliverable_1.tex
          pdflatex docs/design-document/Final_Project_Team_3_Deliverable_1.tex

      - name: Move PDF to Top-Level Repo Folder
        run: |
          cp Final_Project_Team_3_Deliverable_1.pdf ../..

      - name: Calculate Page Count
        id: page_count
        run: |
          PAGES=$(pdfinfo docs/design-document/Final_Project_Team_3_Deliverable_1.pdf | grep Pages | awk '{print $2}')
          echo "PAGES=$PAGES" >> $GITHUB_ENV

      - name: Calculate Word Count
        id: word_count
        run: |
          WORDS=$(pdftotext docs/design-document/Final_Project_Team_3_Deliverable_1.pdf - | wc -w)
          echo "WORDS=$WORDS" >> $GITHUB_ENV

      - name: Configure Git
        run: |
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          git config pull.rebase false

      - name: Push to Remote
        run: |
          git add Final_Project_Team_3_Deliverable_1.pdf
          if git diff --staged --quiet; then
            echo "No changes detected. Skipping commit and push."
          else
            git commit -m "PAGES: ${{ env.PAGES }} | WORDS: ${{ env.WORDS }}"
            git pull origin main
            git push origin main
            echo "Changes committed and pushed successfully."
          fi

      - name: Repository Dispatch
        uses: peter-evans/repository-dispatch@ff45666b9427631e3450c54a1bcbee4d9ff4d7c0 # v3.0.0
        with:
          token: ${{ secrets.AAI540_GROUP3_DISPATCH_TOKEN }}
          repository: aai540-group3/project
          event-type: create-release

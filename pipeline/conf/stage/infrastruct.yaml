# Stage configuration for infrastructure setup
enabled: true
stage: infrastruct
description: "Infrastructure setup stage"

# Metadata configuration
artifacts:
  save_path: ${paths.infrastructure}/metadata.json

# Custom stage parameters can be added here if needed
resources:
  s3:
    enabled: true
  dynamodb:
    enabled: true
  cloudwatch:
    enabled: true